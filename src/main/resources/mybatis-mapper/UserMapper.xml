<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cmt.cmt_eyes.repository.UserRepository">
    <select id="allUser" resultType="UserEntity">
        SELECT *
        FROM user
    </select>
    <select id="loginUser" resultType="UserEntity">
        SELECT *
        FROM user
        WHERE user_id = #{userId} AND approval is not null
    </select>
    <select id="selUser" resultType="UserEntity">
        SELECT *
        FROM user
        WHERE user_id = #{userId}
    </select>
    <select id="selUserList" resultType="UserEntity">
        SELECT *
        FROM user
        WHERE
            <if test="userSortId != null and userSortId != ''">
                user_sort_id = #{userSortId}
            </if>
            <if test="company != null and company != ''">
                company = #{company}
            </if>
    </select>
    <select id="selCompanyList" resultType="UserEntity">
        SELECT user_name , user_id
        FROM user
        WHERE user_sort_id = 2
    </select>
    <update id="approvalUser">
        UPDATE user
        SET approval = now()
        WHERE user_id = #{userId}
    </update>
    <update id="updatePw">
        UPDATE user
        SET user_pw = #{userPw},
            change_date = now()
        WHERE user_id = #{userId} AND approval is not null
    </update>
</mapper>